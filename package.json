{
    "name": "name",
    "description": "description",
    "authors": "author",
    "version": "1.0.0",
    "main": "pathToMain",
    "dependencies": {},
    "scripts": {
        "start": "git stash; git pull; chmod +x ./main && ./main",
        "build": "bash -c 'GOOS=linux GOARCH=amd64 go build -o main' && upx --best --lzma main"
    }
}
