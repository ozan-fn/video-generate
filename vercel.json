{
    "outputDirectory": "dist",
    "builds": [
        {
            "src": "src/server/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/src/server/server.ts"
        }
    ]
}
